import * as React from 'react';
import type { IPiechartProps } from './IPiechartProps';

export default class Piechart extends React.Component<IPiechartProps, {}> {
  public render(): React.ReactElement<IPiechartProps> {
  
    return (
      <section/>
       
    );
  }
}
